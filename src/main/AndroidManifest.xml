<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="hexa.congressApp.com.congress">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="hexa.congressApp.com.congress.activities.legislators.HomeActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="hexa.congressApp.com.congress.activities.legislators.SingleMemberActivity" />
        <activity
            android:name="hexa.congressApp.com.congress.activities.bills.BillsActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name="hexa.congressApp.com.congress.activities.AboutMeActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait" />
        <activity
            android:name="hexa.congressApp.com.congress.activities.comm.CommitteesActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name="hexa.congressApp.com.congress.activities.favourites.FavouriteActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name="hexa.congressApp.com.congress.activities.comm.CommDescriptionsActivity" />
        <activity android:name="hexa.congressApp.com.congress.activities.bills.BillsDiescriptionsActivity"> </activity>
    </application>

</manifest>